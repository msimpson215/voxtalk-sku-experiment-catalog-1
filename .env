# =========================
# VoxTalk Environment File
# =========================

# Set to "true" to run in safe test mode (no OpenAI calls, no credit spend).
# Set to "false" or remove line entirely to use real OpenAI API.
DEV_MODE=true

# Your real OpenAI API key — only needed when DEV_MODE=false.
# In Render, set this as an environment variable in the dashboard (not in file).
OPENAI_API_KEY=sk-your-real-key-here

# Optional: override the default server port (3000)
PORT=3000
